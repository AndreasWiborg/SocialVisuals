# Text Overlay API environment
# Copy this file to env/.text-overlay.prod (or similar) and fill in real secrets before deploying.

# Supabase project configuration (required)
SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role
SUPABASE_SERVICE_KEY=replace-with-service-role

# Storage buckets used for rendered outputs
SUPABASE_OUTPUT_BUCKET=generated
SUPABASE_OUTPUT_PREFIX=generated
SUPABASE_SIGN_EXPIRES=604800

# LLM provider configuration
LLM_PROVIDER=openai
OPENAI_API_KEY=replace-with-openai-key

# Optional: restrict which local directories can be served via /file endpoint
ALLOWED_BG_DIRS=/app/text-overlay/runs

# Fastify port (matches docker-compose exposure)
PORT=3000
